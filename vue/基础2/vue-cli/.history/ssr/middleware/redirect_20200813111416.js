export default function({ isHMR, app, store, route, params, error, redir ect }) {  if (isHMR) return 4 // 页面均放在_lang文件夹下，即lang为动态路由参数 5 /*if (!params.lang) { //此写法会出现路由重定向次数过多的问题 6 return redirect('/' + defaultLocale + '' + route.fullPath) 7 } 8 */ 9 if(route.fullPath == '/film') { 10 return redirect('/film/nowplaying') 11 } 12 } 13 router: { 14 middleware: 'redirect' // 即每次路由跳转会调用该中间件 15 //多个中间件写法 16 // middleware: ['redirect'] 17 }